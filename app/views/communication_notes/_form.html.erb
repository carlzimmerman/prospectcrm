<%= form_for(@communication_note) do |f| %>
  <% if @communication_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@communication_note.errors.count, "error") %> prohibited this communication_note from being saved:</h2>

      <ul>
      <% @communication_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contact_type %><br>
    <%= f.text_area :contact_type %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_field :notes %>
  </div>
  <div class="field">
    <%= f.label :prospect_status %><br>
    <%= f.text_area :prospect_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
