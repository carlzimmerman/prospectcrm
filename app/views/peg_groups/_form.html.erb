<%= form_for(@peg_group) do |f| %>
  <% if @peg_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peg_group.errors.count, "error") %> prohibited this peg_group from being saved:</h2>

      <ul>
      <% @peg_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :firm_name %><br>
    <%= f.text_area :firm_name %>
  </div>
  <div class="field">
    <%= f.label :website %><br>
    <%= f.text_area :website %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
